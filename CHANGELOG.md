Arvudoro now tells you when a new version is out and updates itself with one click.

- When a new release is available, a bar at the top of the window, an item in the tray menu and a notification offer the update; the app restarts into the new version
- Linux: install for your user with one command, no root needed (see the README)
- AUR package: no more duplicate entry in the app menu
